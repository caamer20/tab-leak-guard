# Tab Leak Guard 0.1.2

Fixes a Firefox toolbar popup sizing bug that could show only a narrow vertical scrollbar instead of the resource monitor.

The popup now uses a stable preferred width. Regression checks include opening the actual popup in an isolated Firefox profile.

No permissions, monitoring behavior, stored data, or recovery behavior changed. Automatic recovery remains disabled; recovery actions still require your confirmation.
